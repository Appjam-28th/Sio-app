import {Primary, Gray} from "@/constants/color";
import {fontList} from "@/assets/fonts/fontList";

export const Theme = {
    colors: {
        primary: Primary,
        gray: Gray,
        white: "#fff",
        black: "#000",
    },
    fonts: fontList,
    fontSize: {
        input: 16,
        chat: 12
    },
    width: {
        large: 350
    },
    height: {
        large: 50,
    },
    borderRadius: {
        small: 10
    }

}